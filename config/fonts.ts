import { Lexend } from "next/font/google";

export const fontLexend = Lexend({
  subsets: ["latin"],
  weight: "300",
});

export const fontLexendBold = Lexend({
  subsets: ["latin"],
  weight: "700",
});
